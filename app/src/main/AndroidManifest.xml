<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.atividade1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Atividade1">
        <activity
            android:name=".view.ListaPixActivity"
            android:label="@string/title_activity_lista_pix"
            android:theme="@style/Theme.Atividade1.NoActionBar"></activity>
        <activity android:name=".view.PixActivity" />
        <activity android:name=".view.BotoesActivity" />
        <activity android:name=".view.MenuActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>